(core.define @NEG (core.lambda (operand)
    (core.list.concat operand @("CALL neg"))))

(core.define @POS (core.lambda (operand)
    operand))

(core.define @! (core.lambda (operand)
    (core.list.concat operand @("CALL not"))))

(core.define @not !)

